# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your Atlas credentials
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/lecture-prac?retryWrites=true&w=majority

# Server Port
PORT=4000
